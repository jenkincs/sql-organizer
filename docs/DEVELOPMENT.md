# Development

Requires Node 20+ and VS Code. Run `npm install`, then `npm run lint`, `npm run typecheck`, `npm test`, and `npm run package`. Use F5 in VS Code to launch an Extension Development Host. Tests use fake providers and never call OpenAI.
