import OpenAI from 'openai';
import { AiProvider, ClassificationInput } from './aiProvider';
import { SqlClassification } from '../domain/models';
import { classificationSchema } from './responseValidator';
export class OpenAiProvider implements AiProvider { constructor(private readonly apiKey: string, private readonly model: string, private readonly timeoutMs: number) {} async classify(input: ClassificationInput): Promise<SqlClassification> { if (!this.model) throw new Error('OpenAI model is not configured. Set sqlOrganizer.model and retry Analyze.'); const client = new OpenAI({ apiKey: this.apiKey, timeout: this.timeoutMs, maxRetries: 0 }); const response = await client.chat.completions.create({ model: this.model, response_format: { type: 'json_object' }, messages: [{ role: 'system', content: 'You are SQL Librarian. Return only a JSON object with category, operation, dialect, purpose, suggestedFilename, tables, parameters, risk, riskReasons, confidence and reviewNotes. Never execute SQL or recommend deletion.' }, { role: 'user', content: JSON.stringify(input) }] }); const content = response.choices[0]?.message.content; if (!content) throw new Error('The AI returned an empty response.'); return classificationSchema.parse(JSON.parse(content)); } }
